{
  "name": "lab-async-await",
  "version": "1.0.0",
  "description": "Async/Await Lab - Random Social Media",
  "main": "index.js",
  "scripts": {
    "test": "mocha --timeout 5000 --reporter mocha-junit-reporter test/*.js"
  },
  "devDependencies": {
    "chai": "^4.3.7",
    "mocha": "^10.0.0",
    "mocha-junit-reporter": "^2.0.0"
  }
}
